services:
  dockerfilesamples:
    image: dockerfilesamples-typescript
    build:
      context: .
      dockerfile: ./Dockerfile
    environment:
      NODE_ENV: production
    ports:
      - 8080:8080
